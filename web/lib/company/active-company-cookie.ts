/** Имя cookie активной компании. В отдельном файле (без server-only),
 *  чтобы использовать и на сервере (active-company.ts), и в клиенте (CompanyNav). */
export const ACTIVE_COMPANY_COOKIE = "cl_active_company";
